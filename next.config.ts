import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Set turbopack root to the current project directory to avoid resolving upwards
  // to C:\Users\nites\package-lock.json
 
};

export default nextConfig;
